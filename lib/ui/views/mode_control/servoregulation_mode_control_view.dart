import 'package:flutter/material.dart';

class ServoregulationModeControlView extends StatelessWidget {
  const ServoregulationModeControlView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        // Text("Mode Control"),
        Stack(
          children: <Widget>[
            // Overlay(),
            // Text("Servoregulation"),
            // TargetCard(),
            // TuningCard(),
          ],
        ),
      ],
    );
  }
}
